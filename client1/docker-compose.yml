services:
  client:
    build:
      context: .
    networks:
      vpn_network:
        ipv4_address: 10.0.2.2

networks:
  vpn_network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.2.0/24
